@IsTest
private class LogExpenseActionTest {
    @IsTest
    static void compiles() {
        // Stub per hackathon policy — verification via
        // scripts/verify-uc3-voice-to-linked.apex and -unlinked.apex.
        System.assertEquals(0, 0);
    }
}